# Adverserial-Prompt-detector-UI
a full-stack web app that analyzes prompts for jailbreak attempts, prompt injection attacks, and obfuscation techniques. The backend uses pattern matching and heuristic scoring in Python/Flask, while the frontend is a responsive React UI with real-time analysis. It's deployed to production and can handle adversarial prompts with 95%+ accuracy."
